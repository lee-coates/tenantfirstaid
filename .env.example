# Host path to GCP credentials file for Docker volume mount.
# Copy this file to .env and fill in your path.
# Typically the ADC file created by: gcloud auth application-default login
GCP_CREDENTIALS_FILE=/home/<USERNAME>/.config/gcloud/application_default_credentials.json
